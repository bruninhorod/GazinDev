// const DevValidation = require('../middleware/DevValidation');

// const { nome, sexo, idade, hobby, datanascimento } = req.body;

// describe('Validação do cadastro de desenvolvedor', () => {
//   test('Tem que retornar true se nome estiver preenchido', async () => {
//     expect(nome.validate('teste bruno')).toBe(400);
//   });

// });
